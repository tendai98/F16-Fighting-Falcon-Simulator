# UI Scrollbar / Hint Cleanup

- Styled the Scoreboard / Replays scroller with the cockpit green HUD theme.
- Styled the Flight School lesson list scroller with the same HUD theme.
- Removed the main-menu terrain-scale hint from the client UI.
- Removed the replay-browser lazy-load hint from the client UI.
